# Prompt review: Codex GPT-6 Astra

Observed on 2026-09-04 using the project's local timezone, `America/New_York`.
This is a review record, not a runtime prompt import.

| Field | Value |
|---|---|
| Source repository | `https://github.com/asgeirtj/system_prompts_leaks.git` |
| Source revision | `eb47bcf82b686bc1ea0244442ce31dfa8481d2c5` |
| Candidate | `OpenAI/Codex/gpt-6-astra.md` |
| Baseline | `OpenAI/Codex/gpt-5.6.md` |
| Comparison | unified diff fetched at the pinned revision |
| Runtime files changed | no |

The candidate adds explicit permission handling, authorization persistence,
autonomy/persistence, revised conversation steering and compaction language,
tool and shell mechanics, and a different writing/personality layer. It is
roughly the same size as the baseline; the useful question is which mechanics
belong in this project, not whether the whole file can be copied.

## Decision table

| Change family | Bucket | Main decision | Reason |
|---|---|---|---|
| Permission judgment, previously granted authorization, and finishing already-authorized work | `THINK` | Keep as a review candidate; no sync in this sample | The current base already separates authorization from discussion and asks only when the answer would change the action. Adding persistence would change turn behavior and needs a concrete gap first. |
| Source lookup and evidence before asking for a decision | `SEEK` | Covered; no sync | The current base already routes earlier context, connected data, web, and pointed-to sources to their authoritative source. |
| Autonomy, persistence, and “continue until complete” execution drive | `WORKER-ONLY` | Exclude from Main | This is execution behavior. Putting it in the owner-side base would turn discussion or intent into automatic work and would conflict with the Boss boundary. |
| Skill selection and skill-loading procedure | `SEEK` | Do not import the procedure | The project’s own skill and AGENTS rules define when maintenance is relevant. A generic prompt-wide skill procedure would broaden triggers and duplicate local policy. |
| Conversation steering and compaction continuity | `THINK` | Covered for now; no sync | The current base treats corrections, prior output, continuity, and side problems as context. Revisit only if a captured behavior demonstrates a reproducible owner-side gap. |
| Command escaping, `rg` use, parallel calls, test reporting, and structured handoff | `OPERATE` | Covered in the current base; no sync | These are useful after an action is chosen, and the current base already carries the relevant mechanics. |
| Personality, writing taste, visualization preferences, and generic product plumbing | `IGNORE` | Exclude | This project is not doing another Boss personality pass, and these changes do not establish a runtime compatibility need. |

## Result

`base/main changed: no`.

The Astra capture is now a regression fixture for future prompt-maintenance
reviews. A later review may promote one row into a small, rephrased rule, but it
must name the owner-side behavior it changes and preserve the standing SOURCES
rule:

> Take how its tools are used. Do not take what it thinks it should be doing.
